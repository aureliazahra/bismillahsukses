import 'package:flutter/material.dart';

class RealTimeFacePage extends StatelessWidget {
  const RealTimeFacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Real-Time Face Page",
        style: TextStyle(color: Colors.white, fontSize: 20),
      ),
    );
  }
}
